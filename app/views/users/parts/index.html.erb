<h1>商品一覧</h1>
<p>Find me in app/views/users/parts/index.html.erb</p>

<%= form_with url:search_users_parts_path, method: :get do |f| %>
  <%= f.collection_select(:category, @categories, :id, :name, prompt: '選択してください') %>
  <%= f.submit '検索'%>
<% end %>


<table border="1">
  <tr>
    <th>商品名</th>
    <th>単価</th>
    <th>個数</th>
    <th>カテゴリー</th>
  </tr>
  <% @parts.each do |part| %>
    <tr>
      <td><%= part.name %></td>
      <td><%= part.price %></td>
      <td><%= part.quantity %></td>
      <td><%= part.parts_category.name %></td>
    </tr>
  <% end %>
</table>
